Configuration:
  status: WARN

  Appends:
    Console:
      name: Console
      target: SYSTEM_OUT
      PatternLayout:
        pattern: "%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"

    File:
      name: FileLogger
      fileName: "logs/app.log"
      append: true
      PatternLayout:
        pattern: "%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"

  Loggers:
    Logger:
      - name: com.example
        level: debug
        additivity: false
        AppenderRef:
          - ref: Console
          - ref: FileLogger

    Root:
      level: error
      AppenderRef:
        - ref: Console